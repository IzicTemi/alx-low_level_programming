#include "main.h"
/**
 * Print _putchar, followed by an new line
 *
 * Return: On success 0.
 * On error, 1 is returned, and errno is set appropriately.
 */
int main (void)
{
    char *sh = "_putchar";
	_putchar(*sh);    
	return (0);
}